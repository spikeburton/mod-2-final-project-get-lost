<%= render 'signup' do %>
  <%= form_for @user, url: signup_path do |f| %>
    <%= render 'users/info', f: f %>
    <div class="ui centered grid">
      <div class="row">
        <%= f.submit "Continue", class: 'ui primary button' %>
      </div>
    </div>
  <% end %>
<% end %>
